{
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100
      },
      "dueDate": {
        "type": "string",
        "format": "date"
      }
    },
    "required": ["name", "dueDate"]
  }